# Exercise
-----------------------------------------------------------------------

## Start

Run `npm run start` for start.


## Running unit tests

Run `npm run test:coverage` to execute the unit tests.